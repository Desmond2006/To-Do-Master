<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox alignment="CENTER" spacing="30" xmlns="http://javafx.com/javafx/11"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.todo.views.LoginController"
      style="-fx-background-color: linear-gradient(to right, #667eea 0%, #764ba2 100%);">

    <padding>
        <Insets top="40" right="50" bottom="40" left="50"/>
    </padding>

    <!-- Адаптивный контейнер -->
    <VBox alignment="CENTER" spacing="30" maxWidth="800">

        <!-- Заголовок -->
        <VBox alignment="CENTER" spacing="10">
            <Label text="📝 To-Do Master" style="-fx-text-fill: white; -fx-font-size: 36; -fx-font-weight: bold;"/>
            <Label text="Организуйте свою жизнь" style="-fx-text-fill: white; -fx-font-size: 18; -fx-opacity: 0.9;"/>
        </VBox>

        <!-- Основной контент -->
        <HBox spacing="50" alignment="CENTER" maxWidth="800">

            <!-- Левая часть с приветствием -->
            <VBox spacing="20" prefWidth="350" maxWidth="350">
                <VBox spacing="15">
                    <Label text="Преимущества:" style="-fx-text-fill: white; -fx-font-size: 20; -fx-font-weight: bold;"/>
                    <VBox spacing="10">
                        <HBox spacing="10" alignment="CENTER_LEFT">
                            <Label text="✓" style="-fx-text-fill: white; -fx-font-size: 18;"/>
                            <Label text="Создавайте задачи" style="-fx-text-fill: white; -fx-font-size: 14;"/>
                        </HBox>
                        <HBox spacing="10" alignment="CENTER_LEFT">
                            <Label text="✓" style="-fx-text-fill: white; -fx-font-size: 18;"/>
                            <Label text="Устанавливайте сроки" style="-fx-text-fill: white; -fx-font-size: 14;"/>
                        </HBox>
                        <HBox spacing="10" alignment="CENTER_LEFT">
                            <Label text="✓" style="-fx-text-fill: white; -fx-font-size: 18;"/>
                            <Label text="Создавайте заметки" style="-fx-text-fill: white; -fx-font-size: 14;"/>
                        </HBox>
                        <HBox spacing="10" alignment="CENTER_LEFT">
                            <Label text="✓" style="-fx-text-fill: white; -fx-font-size: 18;"/>
                            <Label text="Полный контроль" style="-fx-text-fill: white; -fx-font-size: 14;"/>
                        </HBox>
                    </VBox>
                </VBox>
            </VBox>

            <!-- Правая часть с формой -->
            <VBox spacing="20" style="-fx-padding: 30; -fx-background-color: white; -fx-background-radius: 12; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.15), 15, 0, 0, 4);"
                  prefWidth="350" maxWidth="350">

                <VBox spacing="5" alignment="CENTER">
                    <Label text="Добро пожаловать!" style="-fx-font-size: 22; -fx-font-weight: bold; -fx-text-fill: #1e293b;"/>
                    <Label text="Войдите в свой аккаунт" style="-fx-font-size: 13; -fx-text-fill: #64748b;"/>
                </VBox>

                <!-- Форма входа -->
                <VBox spacing="15">
                    <!-- Поле ввода логина -->
                    <VBox spacing="5">
                        <Label text="Имя пользователя" style="-fx-font-weight: bold; -fx-text-fill: #475569; -fx-font-size: 13;"/>
                        <TextField fx:id="usernameField" promptText="Введите логин"
                                   style="-fx-padding: 10; -fx-background-radius: 6; -fx-border-color: #e2e8f0; -fx-border-radius: 6; -fx-font-size: 14;"/>
                    </VBox>

                    <!-- Поле ввода пароля -->
                    <VBox spacing="5">
                        <Label text="Пароль" style="-fx-font-weight: bold; -fx-text-fill: #475569; -fx-font-size: 13;"/>
                        <PasswordField fx:id="passwordField" promptText="Введите пароль"
                                       style="-fx-padding: 10; -fx-background-radius: 6; -fx-border-color: #e2e8f0; -fx-border-radius: 6; -fx-font-size: 14;"/>
                    </VBox>

                    <!-- Запомнить меня -->
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <CheckBox fx:id="rememberCheckBox" text="Запомнить меня"
                                  style="-fx-text-fill: #475569; -fx-font-size: 13;"/>
                    </HBox>

                    <!-- Кнопка входа -->
                    <Button fx:id="loginButton" text="Войти"
                            onAction="#handleLogin"
                            style="-fx-background-color: #3b82f6; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 15; -fx-padding: 12; -fx-background-radius: 6; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(59,130,246,0.3), 8, 0, 0, 2);"
                            maxWidth="Infinity"/>

                    <!-- Сообщение об ошибке -->
                    <Label fx:id="messageLabel" wrapText="true" maxWidth="300"
                           style="-fx-text-fill: #ef4444; -fx-font-size: 12; -fx-padding: 5 0;"
                           visible="false"/>

                    <!-- Разделитель -->
                    <HBox alignment="CENTER" spacing="8">
                        <Separator prefWidth="80"/>
                        <Label text="или" style="-fx-text-fill: #94a3b8; -fx-font-size: 12;"/>
                        <Separator prefWidth="80"/>
                    </HBox>

                    <!-- Ссылка на регистрацию -->
                    <HBox alignment="CENTER" spacing="5">
                        <Label text="Нет аккаунта?" style="-fx-text-fill: #64748b; -fx-font-size: 13;"/>
                        <Button fx:id="registerButton" text="Зарегистрироваться"
                                onAction="#handleRegister"
                                style="-fx-background-color: transparent; -fx-text-fill: #3b82f6; -fx-font-weight: bold; -fx-padding: 0 5; -fx-cursor: hand; -fx-font-size: 13;"/>
                    </HBox>
                </VBox>

            </VBox>

        </HBox>

        <!-- Быстрый вход (опционально) -->
        <VBox alignment="CENTER" spacing="10" visible="false" managed="false">
            <Label text="Быстрый вход:" style="-fx-text-fill: rgba(255,255,255,0.8); -fx-font-size: 14;"/>
            <HBox spacing="15" alignment="CENTER">
                <Button text="Demo User"
                        style="-fx-background-color: rgba(255,255,255,0.2); -fx-text-fill: white; -fx-padding: 8 20; -fx-background-radius: 20;"
                        onAction="#handleLogin"/>
            </HBox>
        </VBox>

    </VBox>

    <!-- Футер -->
    <VBox alignment="CENTER" spacing="5">
        <Label text="© 2024 To-Do Master"
               style="-fx-text-fill: rgba(255,255,255,0.7); -fx-font-size: 12;"/>
        <Label text="Версия 1.0.0"
               style="-fx-text-fill: rgba(255,255,255,0.5); -fx-font-size: 11;"/>
    </VBox>

</VBox>