<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.DatePicker?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.VBox?>

<AnchorPane prefHeight="600.0" prefWidth="900.0" style="-fx-background-color: #ffffff;" xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.todo.views.AllTasksViewController">

    <children>
        <VBox spacing="20" AnchorPane.bottomAnchor="0.0" AnchorPane.leftAnchor="0.0" AnchorPane.rightAnchor="0.0" AnchorPane.topAnchor="0.0">
            <padding>
                <Insets bottom="20" left="20" right="20" top="20" />
            </padding>

            <!-- Заголовок и статистика -->
            <HBox alignment="CENTER_LEFT" spacing="15">
                <Label style="-fx-font-size: 24; -fx-font-weight: bold; -fx-text-fill: #1e293b;" text="📭 Все задачи" />
                <Label fx:id="tasksCountLabel" style="-fx-text-fill: #64748b; -fx-font-size: 14;" text="0 задач" />
                <Region HBox.hgrow="ALWAYS" />
                <Button fx:id="btnAddTask" onAction="#addNewTask" style="-fx-background-color: #3b82f6; -fx-text-fill: white; -fx-background-radius: 8; -fx-padding: 8 15;" text="➕ Новая задача" />
            </HBox>

            <!-- Поле для добавления новой задачи -->
            <VBox fx:id="addTaskBox" spacing="10" visible="false">
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <TextField fx:id="newTaskField" promptText="Введите новую задачу..." style="-fx-background-radius: 8; -fx-border-radius: 8; -fx-padding: 10;" HBox.hgrow="ALWAYS" />
                </HBox>
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Label text="Дата выполнения:" style="-fx-text-fill: #64748b; -fx-font-size: 12;"/>
                    <DatePicker fx:id="deadlinePicker" style="-fx-background-radius: 8; -fx-pref-width: 150;" />
                    <Region HBox.hgrow="ALWAYS" />
                    <Button fx:id="btnSaveTask" onAction="#saveTask" style="-fx-background-color: #10b981; -fx-text-fill: white; -fx-background-radius: 8;" text="Добавить" />
                    <Button fx:id="btnCancelTask" onAction="#cancelAddTask" style="-fx-background-color: #ef4444; -fx-text-fill: white; -fx-background-radius: 8;" text="Отмена" />
                </HBox>
            </VBox>

            <!-- Фильтры и поиск -->
            <HBox alignment="CENTER_LEFT" spacing="15">
                <ComboBox fx:id="filterComboBox" promptText="Все задачи" style="-fx-background-radius: 8; -fx-pref-width: 150;" />
                <TextField fx:id="searchField" promptText="Поиск задач..." style="-fx-background-radius: 8; -fx-pref-width: 200;" />
                <Region HBox.hgrow="ALWAYS" />
                <Label fx:id="completedCountLabel" style="-fx-text-fill: #64748b;" text="Выполнено: 0" />
            </HBox>

            <!-- Список задач -->
            <ScrollPane fitToWidth="true" style="-fx-background-color: transparent; -fx-border-color: transparent;">
                <VBox fx:id="tasksContainer" spacing="8" />
            </ScrollPane>
        </VBox>
    </children>
</AnchorPane>