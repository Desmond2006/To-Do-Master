<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox alignment="TOP_CENTER" spacing="15" xmlns="http://javafx.com/javafx/11"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.todo.views.RegisterController"
      style="-fx-background-color: linear-gradient(to right, #10b981 0%, #3b82f6 100%);">

    <padding>
        <Insets top="25" right="20" bottom="25" left="20"/>
    </padding>

    <!-- Заголовок -->
    <VBox alignment="CENTER" spacing="5">
        <Label text="📝 To-Do Master" style="-fx-text-fill: white; -fx-font-size: 24; -fx-font-weight: bold;"/>
        <Label text="Создание аккаунта" style="-fx-text-fill: white; -fx-font-size: 14; -fx-opacity: 0.9;"/>
    </VBox>

    <!-- Компактная форма -->
    <VBox spacing="12" style="-fx-padding: 20; -fx-background-color: white; -fx-background-radius: 8; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 8, 0, 0, 2);"
          maxWidth="380">

        <!-- Имя пользователя -->
        <VBox spacing="2">
            <Label text="Логин *" style="-fx-font-weight: bold; -fx-text-fill: #475569; -fx-font-size: 12;"/>
            <TextField fx:id="usernameField" promptText="мин. 3 символа"
                       style="-fx-padding: 8; -fx-background-radius: 4; -fx-border-color: #e2e8f0; -fx-border-radius: 4; -fx-font-size: 13;"/>
            <Label fx:id="usernameError" style="-fx-text-fill: #ef4444; -fx-font-size: 10;" visible="false"/>
        </VBox>

        <!-- Email -->
        <VBox spacing="2">
            <Label text="Email (необязательно)" style="-fx-font-weight: bold; -fx-text-fill: #475569; -fx-font-size: 12;"/>
            <TextField fx:id="emailField" promptText="email@example.com"
                       style="-fx-padding: 8; -fx-background-radius: 4; -fx-border-color: #e2e8f0; -fx-border-radius: 4; -fx-font-size: 13;"/>
            <Label fx:id="emailError" style="-fx-text-fill: #ef4444; -fx-font-size: 10;" visible="false"/>
        </VBox>

        <!-- Пароль -->
        <VBox spacing="2">
            <Label text="Пароль *" style="-fx-font-weight: bold; -fx-text-fill: #475569; -fx-font-size: 12;"/>
            <PasswordField fx:id="passwordField" promptText="мин. 6 символов"
                           style="-fx-padding: 8; -fx-background-radius: 4; -fx-border-color: #e2e8f0; -fx-border-radius: 4; -fx-font-size: 13;"/>
            <Label fx:id="passwordError" style="-fx-text-fill: #ef4444; -fx-font-size: 10;" visible="false"/>
        </VBox>

        <!-- Подтверждение пароля -->
        <VBox spacing="2">
            <Label text="Подтверждение *" style="-fx-font-weight: bold; -fx-text-fill: #475569; -fx-font-size: 12;"/>
            <PasswordField fx:id="confirmPasswordField" promptText="повторите пароль"
                           style="-fx-padding: 8; -fx-background-radius: 4; -fx-border-color: #e2e8f0; -fx-border-radius: 4; -fx-font-size: 13;"/>
            <Label fx:id="confirmError" style="-fx-text-fill: #ef4444; -fx-font-size: 10;" visible="false"/>
        </VBox>

        <!-- Требования к паролю (компактно) -->
        <HBox spacing="10" alignment="CENTER_LEFT" style="-fx-padding: 5; -fx-background-color: #f8fafc; -fx-background-radius: 4;">
            <Label text="🔒" style="-fx-font-size: 12;"/>
            <Label text="Пароль: минимум 6 символов" style="-fx-text-fill: #64748b; -fx-font-size: 10;"/>
        </HBox>

        <!-- Кнопка регистрации -->
        <Button fx:id="registerButton" text="Зарегистрироваться"
                onAction="#handleRegister"
                style="-fx-background-color: #10b981; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 13; -fx-padding: 9; -fx-background-radius: 4; -fx-cursor: hand;"
                maxWidth="Infinity"/>

        <!-- Сообщение -->
        <Label fx:id="messageLabel" wrapText="true"
               style="-fx-font-size: 11; -fx-padding: 3 0;"
               visible="false"/>

        <!-- Разделитель и ссылка на вход -->
        <VBox spacing="8" alignment="CENTER">
            <Separator/>
            <HBox alignment="CENTER" spacing="5">
                <Label text="Уже есть аккаунт?" style="-fx-text-fill: #64748b; -fx-font-size: 11;"/>
                <Button fx:id="loginButton" text="Войти"
                        onAction="#handleLogin"
                        style="-fx-background-color: transparent; -fx-text-fill: #3b82f6; -fx-font-weight: bold; -fx-padding: 0; -fx-cursor: hand; -fx-font-size: 11;"/>
            </HBox>
        </VBox>

    </VBox>

    <!-- Футер -->
    <VBox alignment="CENTER" spacing="1">
        <Label text="© 2024 To-Do Master"
               style="-fx-text-fill: rgba(255,255,255,0.7); -fx-font-size: 9;"/>
        <Label text="Версия 1.0.0"
               style="-fx-text-fill: rgba(255,255,255,0.5); -fx-font-size: 8;"/>
    </VBox>

</VBox>